package book_super_inheritance.problem5_final_super;

public class TestToExtentFinalClass{
   /* public TestToExtentFinalClass(char[] hash) {
        super(hash);
    }*/
    //if we try to extend Password class then it will throw an error...final class can't be inherited.
}
